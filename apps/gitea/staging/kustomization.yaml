apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
  - ../base
patches:
  - patch: |-
      - op: replace
        path: /spec/values/ingress/hosts/0/host
        value: gitea.staging.local
    target:
      kind: HelmRelease
      name: gitea